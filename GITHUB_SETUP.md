# 🚀 GitHub Actions - Автоматичні оновлення

Так, проект **повністю готовий** для запуску на GitHub! Ось як це налаштувати:

## 📋 Що вам потрібно

1. **GitHub акаунт** (безплатний)
2. **Форк цього репозиторію** або власний з цим кодом
3. **Читати інструкції** нижче 👇

## 🚀 Налаштування (3 кроки)

### 1️⃣ Завантажте проект на GitHub

```bash
# Локально
git init
git add .
git commit -m "Initial commit"
git branch -M main
git remote add origin https://github.com/ВАШ_НІКНЕЙМ/dtek-calendar.git
git push -u origin main
```

### 2️⃣ Разрез GitHub Actions

Workflows вже готові! Вони автоматично запустяться при push.

Перейдіть на: `https://github.com/ВАШ_НІКНЕЙМ/dtek-calendar/actions`

### 3️⃣ Дозволите GitHub Actions

1. Перейдіть на **Settings** вашого репозиторію
2. Виберіть **Actions** → **General**
3. Виберіть **"Allow all actions and reusable workflows"**
4. Натисніть **Save**

## 📅 Що відбувається автоматично

### Workflow 1: `update.yml` (Автоматичні оновлення)

```
⏰ Запускається: кожні 15 хвилин
🎯 Що робит:
  1. Завантажує останній код
  2. Встановлює Node.js і залежності
  3. Запускає скрипт (отримує дані з ДТЕК)
  4. Якщо дані змінилися → закомітює оновлення
  5. Зберігає календар (артефакт на 30 днів)
```

### Workflow 2: `generate-custom.yml` (Вручну з фреш адресою)

```
🎯 Запуск вручну через GitHub UI:
  1. Перейти на Actions → "Generate Calendar (Custom)"
  2. Натиснути "Run workflow"
  3. Ввести адресу (місто, вулиця, будинок)
  4. Натиснути "Run workflow"
  5. Розпочнеться генерація календаря
```

## 📊 Моніторинг

### Переглянути запуски

1. Перейдіть на вкладку **Actions**
2. Виберіть workflow
3. Перегляньте логи

**Статуси:**
- ✅ **Success** - все ОК
- ❌ **Failed** - виникла помилка (див. логи)
- ⏳ **In Progress** - зараз виконується

### Завантажити календар

Після успішного запуску:

1. Перейдіть на сторінку запуску
2. Прокрутіть вниз до **Artifacts**
3. Натисніть на `dtek-calendar` або `dtek-calendar-custom`
4. Завантажиться файл `dtek.ics`

## 🔧 Налаштування

### Змінити розклад оновлень

Відредагуйте файл `.github/workflows/update.yml`:

```yaml
on:
  schedule:
    - cron: '*/30 * * * *'  # Наприклад, кожні 30 хвилин
```

### Змінити адресу за замовчуванням

Відредагуйте файл `config.json`:

```json
{
  "address": {
    "city": "ВАШ ГОРОД",
    "street": "ВАША УЛИЦЯ",
    "house": "НОМЕР"
  }
}
```

## 💾 Збереження календаря

### Варіант 1: Auto-commit в GitHub (простий)
Workflow автоматично закомітить оновлення календаря. Ви завжди можете переглянути історію через GitHub.

### Варіант 2: Завантажити як артефакт
Кожен запуск зберігає календар як артефакт на 30 днів.

### Варіант 3: Синхронізувати з хмарою
```bash
# Після генерації, синхронізуйте з Google Drive, Dropbox, тощо
cp dtek.ics ~/Google\ Drive/DTEK-Calendar/dtek.ics
```

## 🔐 Важливо!

### Для публічного репозиторію
⚠️ **Не комітьте секретні дані!** 

Натомість використовуйте GitHub Secrets:

```yaml
- name: ⚙️ Налаштувати конфіг
  run: |
    cat > config.json << EOF
    {
      "address": {
        "city": "${{ secrets.DTEK_CITY }}",
        "street": "${{ secrets.DTEK_STREET }}",
        "house": "${{ secrets.DTEK_HOUSE }}"
      }
    }
    EOF
```

Потім додайте Secrets в Settings → Secrets and variables → Actions

## ❓ Проблеми?

### Workflow не запускається

1. Перевірте, чи дозволени GitHub Actions в Settings
2. Перевірте, чи є workflow файли в `.github/workflows/`
3. Перевірте логи в Actions

### Помилка "Playwright not found"

Залежності встановлюються автоматично через `npm ci`. Якщо помилка персистує:

```yaml
- name: 🔧 Встановити залежності
  run: npm ci --legacy-peer-deps
```

### Помилка комміту

GitHub Actions не може закомітити, якщо не дозволено. Додайте перквіз:

```yaml
- name: 🔐 Налаштувати Git
  run: |
    git config --global user.email "action@github.com"
    git config --global user.name "GitHub Action"
```

## 📈 Статистика

**Безплатний ліміт GitHub Actions:**
- 🕐 **2,000 minutes/month** для приватних репозиторіїв
- Мій workflow займає ~3-5 хвилин на запуск
- За 15 хвилин: 96 запусків/день
- За місяць: ~2,880 запусків (займає ~8,640-14,400 хвилин)

⚠️ **Це перевищує ліміт!** Для частих запусків потрібно **збільшити GitHub Actions хвилини** або **використовувати публічний репозиторій** (безлімітно).

**Рекомендація:** Встановіть розклад на **кожні 60 хвилин** замість 15:
```yaml
- cron: '0 * * * *'  # Один раз на годину
```

## 🌍 Публічне поширення

Якщо ви хочете поширити це як публічний сервіс:

1. **Форк на GitHub**
2. **Налаштувати Actions**
3. **Люди можуть форкити ваш репозиторій**
4. **Налаштовувати свою адресу в config.json**
5. **Включити Actions у своєму форку**
6. **Календар оновлюватиметься автоматично!**

---

**Готово! Тепер календар буде оновлюватися на GitHub автоматично.** ⚡

Питання? Перевірте логи в Actions або створіть Issue.
