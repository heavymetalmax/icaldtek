# 🚀 GitHub Actions Setup

Цей проект використовує **GitHub Actions** для автоматичного оновлення календаря кожні 15 хвилин.

## 📋 Конфігурація

### Workflow: `update.yml`

Розташований в `.github/workflows/update.yml`

**Розклад виконання:**
- ⏰ Запускається кожні **15 хвилин** (`*/15 * * * *`)
- 🎯 Може запуститися вручну через GitHub UI

### Що робить workflow

1. **Перевіряє репозиторій** - завантажує останній код
2. **Встановлює Node.js** - версія 18
3. **Встановлює залежності** - npm пакети та Playwright браузер
4. **Запускає скрипт** - `node index.js` отримує дані з ДТЕК
5. **Перевіряє зміни** - порівнює новий календар зі старим
6. **Закомітити та push** - якщо календар змінився, отримує оновлення
7. **Завантажує артефакт** - зберігає копію календаря (30 днів)

## 📊 Моніторинг

### Перегляд логів

1. Перейдіть на сторінку репозиторію GitHub
2. Виберіть вкладку **"Actions"**
3. Нижче буде список усіх запусків workflow

### Статус workflow

- ✅ **Success** - календар успішно оновлено
- ❌ **Failed** - виникла помилка (перевіриз логи)
- ⏳ **In Progress** - workflow зараз виконується

### Логи виконання

Натисніть на конкретний запуск, щоб переглянути детальні логи.

## 🔧 Налаштування

### Змінити розклад оновлень

Відредагуйте файл `.github/workflows/update.yml`:

```yaml
on:
  schedule:
    - cron: '*/30 * * * *'  # Наприклад, кожні 30 хвилин
```

**Формат cron:**
```
┌───────────── хвилина (0 - 59)
│ ┌───────────── година (0 - 23)
│ │ ┌───────────── день місяця (1 - 31)
│ │ │ ┌───────────── місяць (1 - 12)
│ │ │ │ ┌───────────── день тижня (0 - 6) (Неділя = 0)
│ │ │ │ │
│ │ │ │ │
* * * * *
```

Приклади:
- `0 * * * *` - щогодини
- `*/15 * * * *` - кожні 15 хвилин
- `0 0 * * *` - один раз на день (в 00:00)
- `0 9 * * 1-5` - кожний буденний день в 9:00

## 📦 Артефакти

Кожен запуск workflow зберігає календар як **GitHub Artifact** на 30 днів.

Для завантаження:
1. Перейдіть на вкладку **"Actions"**
2. Виберіть потрібний запуск
3. У розділі **"Artifacts"** натисніть на `dtek-calendar`

## 🔐 Безпека

Workflow використовує:
- ✅ Стандартні GitHub Action токени
- ✅ Автоматична аутентифікація для push
- ✅ Коротки таймаути (30 хвилин максимум)

## ⚠️ Важливо

GitHub Actions мають обмеження:

- ⏱️ Максимум **3,000 workflow запусків на місяць** (для безплатних акаунтів)
- 🧠 Максимум **20 одночасних запусків**
- ⚡ Максимум **35 днів** історії логів

При запуску кожні 15 хвилин буде приблизно **96 запусків на день** = **2,880 запусків на місяць** ✅

## 🚨 Розв'язання проблем

### Workflow не запускається

1. Переконайтеся, що GitHub Actions **увімкнено**:
   - Settings → Actions → General → Allow all actions and reusable workflows

2. Перевіріть cron розклад в `update.yml`

3. Перевіріть логи вручну:
   - Actions → Update DTEK Calendar → Run workflow

### Помилка при встановленні залежностей

```
npm ERR! Could not resolve dependency
```

Розв'язання:
```bash
npm ci  # Замість npm install
```

### Скрипт зависає або часто помилюється

Збільшіть `timeout-minutes`:

```yaml
jobs:
  update-calendar:
    timeout-minutes: 45  # Замість 30
```

### Календар не оновлюється

1. Перевіріть, чи змінилися селектори на сайті ДТЕК
2. Запустіть workflow вручну через GitHub UI
3. Переглянете логи для деталей помилки

## 📞 Контакти ДТЕК

- 📱 Сайт: https://www.dtek-krem.com.ua/
- 🔗 Розклад: https://www.dtek-krem.com.ua/ua/shutdowns

## 📚 Документація

- [GitHub Actions Documentation](https://docs.github.com/en/actions)
- [Cron Syntax](https://crontab.guru/)
- [Workflow Syntax](https://docs.github.com/en/actions/using-workflows/workflow-syntax-for-github-actions)

---

**Налаштовано на:** 30 січня 2026 р.
