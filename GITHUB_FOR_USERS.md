# 🌐 Розповсюдження на GitHub

Ваш проект **повністю готовий** для GitHub! Ось як його розповсюджувати:

## 📋 Варіант 1: Особистий проект (для себе)

### 1. Створіть репозиторій на GitHub

1. Перейдіть на [github.com/new](https://github.com/new)
2. Назва: `dtek-calendar` (або інша)
3. Опис: "Автоматичний генератор календаря розкладу відключень ДТЕК"
4. Виберіть **Public** (щоб інші могли форкити)
5. **Створити репозиторій**

### 2. Завантажте код

```bash
cd /Users/max/icaldtek
git remote add origin https://github.com/ВАШ_НІКНЕЙМ/dtek-calendar.git
git branch -M main
git push -u origin main
```

### 3. Включіть GitHub Actions

1. Перейдіть на Settings репозиторію
2. Виберіть **Actions** → **General**
3. Виберіть **"Allow all actions and reusable workflows"**
4. **Save**

### 4. Тестуйте!

1. Перейдіть на вкладку **Actions**
2. Workflow `update.yml` запустився автоматично
3. Переглянути логи

**Готово! Календар оновлюватиметься автоматично на GitHub.** ⚡

---

## 📋 Варіант 2: Розповсюдження для інших

Якщо ви хочете поділитися з іншими:

### 1. Додайте інструкцію

Створіть `GITHUB_FOR_USERS.md`:

```markdown
# Як використовувати на GitHub

1. **Fork репозиторію** (кнопка Fork вгорі)
2. **Перейти на Settings → Actions** → дозволити Actions
3. **Змінити адресу** в config.json на свою
4. **Commit & Push** (або GitHub Actions запустить автоматично)
5. **Календар буде оновлюватися автоматично!**

## Де взяти календар?

- Вкладка **Actions** → переглянути запуски
- Завантажити артефакт `dtek-calendar`
- Імпортувати в календар
```

### 2. Розповсюдіть посилання

Поділіться посиланням на репозиторій:
```
https://github.com/ВАШ_НІКНЕЙМ/dtek-calendar
```

### 3. Люди форкують

1. Натискають **Fork**
2. Налаштовують свою адресу
3. Їхні Actions запускаються автоматично
4. Вони можуть завантажити календар з артефактів

---

## 🎯 GitHub Actions особливості

### ✅ Що вже налаштовано

| Файл | Призначення |
|------|-----------|
| `.github/workflows/update.yml` | Автоматичне оновлення кожні 15 хв |
| `.github/workflows/generate-custom.yml` | Запуск з користувацькою адресою |

### ⚙️ Запуск вручну

Люди можуть натиснути "Run workflow" вручну:

1. Перейти на Actions
2. Виберіть "Generate Calendar (Custom)"
3. Натисніть "Run workflow"
4. Введіть адресу
5. Натисніть "Run workflow"

**Календар генерується за ~3-5 хвилин!**

---

## 🔐 Для публічного репозиторію

Якщо помітне, що конфіг містить адресу - це ОК! Це публічна інформація про адресу.

Але якщо хочете приховати:

### Використати GitHub Secrets

1. Settings → Secrets and variables → Actions
2. New repository secret:
   - `DTEK_CITY` = "с. Гора"
   - `DTEK_STREET` = "вул. Мостова"
   - `DTEK_HOUSE` = "21"

3. В workflow можна використовувати:
```yaml
- name: Configure
  run: echo "${{ secrets.DTEK_CITY }}"
```

---

## 📊 Статистика GitHub Actions

### Безплатні хвилини

- **Публічний репозиторій:** ∞ (невелике число)
- **Приватний репозиторій:** 2,000 хвилин/місяц

### Мій workflow

- **Час виконання:** ~3-5 хвилин
- **Розклад:** кожні 15 хвилин = 96 запусків/день
- **За місяць:** ~2,880 запусків = 8,640-14,400 хвилин

⚠️ **Для приватного репозиторію:** Зміниьте розклад на кожні 60 хвилин:

```yaml
- cron: '0 * * * *'  # Один раз на годину
```

---

## 🚀 Приклади використання

### Для домашнього використання

```bash
# 1. Fork на GitHub
# 2. Clone локально
git clone https://github.com/ВАШ_НІКНЕЙМ/dtek-calendar.git
cd dtek-calendar

# 3. Змінити адресу
echo '{"address":{"city":"м. Київ","street":"вул. Хрещатик","house":"1"}}' > config.json

# 4. Commit & Push
git add config.json
git commit -m "Update address"
git push

# 5. GitHub Actions запустяться автоматично!
```

### Для сімейного використання

1. Один репозиторій на родину
2. Кілька гілок (branches) для різних адрес:
   - `main` - основна адреса
   - `office` - офісна адреса
   - `cottage` - дачна адреса
3. Кожна гілка має свій config.json

### Для організацій

1. Один центральний репозиторій
2. GitHub Actions запускаються для всіх адрес
3. Календарі зберігаються в різних артефактах
4. Можна автоматично відправляти по email

---

## 📝 Файли для GitHub

```
dtek-calendar/
├── .github/workflows/
│   ├── update.yml              ← Автоматичне оновлення
│   └── generate-custom.yml     ← Вручну з адресою
├── config.json                 ← Адреса (редагуйте це!)
├── config.example.json         ← Приклад
├── index.js                    ← Скрипт
├── package.json                ← Залежності
├── README.md                   ← Інструкція
├── GITHUB_SETUP.md             ← GitHub Actions гайд
└── .gitignore                  ← Виключення для git
```

---

## ✅ Чек-лист для GitHub

- [x] `.github/workflows/` структура готова
- [x] `update.yml` налаштований
- [x] `generate-custom.yml` налаштований
- [x] `.gitignore` виключає лишні файли
- [x] `config.json` в .gitignore ✓
- [x] `dtek.ics` в .gitignore ✓
- [x] Документація готова
- [x] GitHub Actions дозволені

---

**Проект готовий до публікації на GitHub!** 🎉

Відправте посилання, і люди зможуть використовувати: `node index.js` або GitHub Actions.

Питання? [Дивіться GITHUB_SETUP.md](GITHUB_SETUP.md)
